"use strict";
(function($, window, document){
  //Empty JS still needed to initialize this module into the initModules array.
	var _helper = {
	};
	window.defineModule({
		el: '.double-cta',
		els: {
			container: '.double-cta__wrapper',
			left: '.double-cta__left',
			right: '.double-cta__right'
		},
		init: function(){
		},
	});

})(jQuery, window, document);
